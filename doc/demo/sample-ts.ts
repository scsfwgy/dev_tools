interface User{id:number;name:string;age?:number;email?:string}type Status='active'|'inactive'|'pending';const users:User[]=[{id:1,name:'Tom',age:25},{id:2,name:'Jerry'}];function findUser(id:number):User|undefined{return users.find(u=>u.id===id)}async function fetchUser(id:number):Promise<User>{const res=await fetch(`/api/users/${id}`);if(!res.ok)throw new Error('Failed');return res.json()}export{users,findUser,fetchUser,User,Status}
